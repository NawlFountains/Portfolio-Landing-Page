export type Project = {
    name: string
    description: string
    stack: string[]
    repo_link: string
    live_demo_link: string
}

export type Education = {
    title: string
    institution_name: string
    description: string
    place: string
    time: string
}